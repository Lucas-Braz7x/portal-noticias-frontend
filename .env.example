# API do backend (Server Components)
API_URL=http://localhost:3000/api/v1

# URL pública do frontend (SEO, links absolutos)
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Invalidação ISR on-demand (mesmo valor no backend REVALIDATE_SECRET)
REVALIDATE_SECRET=
